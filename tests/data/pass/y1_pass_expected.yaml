config:
  paths:
    - /path/to/{file1,file2}
  patterns:
    - "*.{html,css,js}"
    - "*.{py,pyc}"
data:
  values:
    - value1
    - value2
config_patterns:
  paths:
    - /path/to/{file1,file2}
  patterns:
    - "*.{html,css,js}"
    - "*.{py,pyc}"
